# Install numpy first (required before mediapipe)
numpy==1.26.4

# Core Flask dependencies
Flask==3.0.0
flask-cors==4.0.0
Flask-Mail==0.10.0
gunicorn==21.2.0
Werkzeug==3.0.1

# Authentication & Security
PyJWT==2.8.0
python-dotenv==1.0.1

# Database
mysql-connector-python==8.3.0

# Image Processing & ML (pinned for facenet-pytorch compatibility)
Pillow==10.2.0
opencv-python-headless==4.10.0.84
mediapipe==0.10.14

# PyTorch CPU-only (smaller footprint for deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2

# Face recognition (install after Pillow to use existing version)
facenet-pytorch==2.6.0

# Vector Database
pinecone-client==5.0.1
